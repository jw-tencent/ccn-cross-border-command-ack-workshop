# Secrets and certificates
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.pfx
*.p12

# Deployment-local endpoint configuration
# The deployed runtime config is generated outside the source checkout or copied
# into the static web root after deployment. Never track it.
app/runtime-config.js
app/runtime-config.local.js
app/runtime-config.production.js

# Terraform state and local provider data
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfplan.*
plan.out
crash.log
crash.*.log
terraform.tfvars
*.auto.tfvars
!infra/terraform/terraform.tfvars.example

# Dependencies and editor files
node_modules/
.DS_Store
.vscode/
.idea/
